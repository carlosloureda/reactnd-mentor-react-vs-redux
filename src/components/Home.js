import React from "react";

const Home = () => (
  <div>
    <h1>Home page</h1>
    <div>
      This is a quick boilerplate page for understanding redux store vs react
      store
    </div>
    <div>
      You will see that we have a form in the user settings page and some info
      of that form is accessed from all the other pages
    </div>
  </div>
);
export default Home;
