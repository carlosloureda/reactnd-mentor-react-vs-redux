import React from "react";

const Footer = () => (
  <footer className="footer">
    <div className="container">
      <span className="text-muted">Footer of the page.</span>
    </div>
  </footer>
);

export default Footer;
